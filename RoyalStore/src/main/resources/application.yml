spring:
  data:
    mongodb:
      host: mongodb
      database: store
      authentication-database: store
      uri: mongodb://root:7skk7QENyt4ZA3YCarktQdp6M4d6wykx@mongodb:27017/store?authSource=admin
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_OAUTH2_CLIENT_ID}
            client-secret: ${GOOGLE_OAUTH2_CLIENT_SECRET}
        provider:
          google:
            issuerUri: https://accounts.google.com

server:
  port: 8080

logging:
  level:
    org:
      springframework:
        security: DEBUG
